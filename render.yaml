services:
  - type: web
    name: lab-ocr-api
    env: ubuntu
    plan: free
    buildCommand: ./render_install.sh && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"